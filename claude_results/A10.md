# A10 Result — IS DISTINCT FROM

## Verdict

**Already fixed — confirmed by both agents (deep coverage).** No source change;
regression tests only. **Final item (13/13).**

The checklist claims `IS DISTINCT FROM` throws "Expression not yet supported".
On the consolidated baseline (`final/main` `3ba2015`) it is fully wired
(`planner.rs` → `evaluator.rs` `evaluate_is_distinct_from` → `type_inference.rs`)
and evaluates correctly.

Baseline: branch `fix/claude-v334-a10` from `final/main` `3ba2015`.

## Coverage

Given that A5/A7/A11 all flipped from "already-fixed" once tested at depth, A10
was probed on the NULL/param surfaces — and stayed green for both agents:

- **Claude** (`tests/v334_a10_is_distinct_from.rs`, 2 tests): full NULL truth
  table for `IS DISTINCT FROM` (`5,5`→false; `5,7`→true; `5,NULL`→true;
  `NULL,NULL`→false) and `IS NOT DISTINCT FROM` (exact complement); in a `WHERE`
  clause; and parameterized `a IS DISTINCT FROM $1` / `IS NOT DISTINCT FROM $1`.
- **Codex** (superset integrated): scalar truth table, table projection + WHERE,
  `query_params` with `Value::Null` on either side, mixed string/int coercion,
  `ON CONFLICT DO UPDATE ... WHERE existing IS DISTINCT FROM excluded`, and a
  300-row COUNT stress (~2 ms).

## Verification (final main)

- `cargo test --test v334_a10_is_distinct_from`: PASS.
- ada_bug_report 3/3, parameterized_query_tests 28/28.
- `cargo test --lib`: 1819 pass / 1 pre-existing hnsw / 1 ignored.
- fmt PASS; clippy only the pre-existing `streaming.rs:69`.

## Quality

Both ~90. Cross-agent value: two independent deep harnesses (incl. coercion +
ON CONFLICT integration + stress) confirming the already-fixed verdict — much
stronger than a single shallow pass, which is the trap A5/A7 fell into.
