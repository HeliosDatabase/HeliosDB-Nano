# T3 Result — CTE + `$N` parameter binding (CONFIRMED already-fixed)

## Verdict

**Already fixed — confirmed by both agents with deep coverage.** No source change;
regression tests only. This is the first genuinely already-fixed item after seven
real bugs (A8, A14, A15, A5, A1, A7, T2).

## Why we trust the already-fixed verdict here (unlike A5/A7)

A5 and A7 both flipped from "already-fixed" to real bugs once tested on the
parameterized / at-scale path. For T3 we therefore tested **well past** the
minimal case and both agents independently stayed green:

- **Claude** (`v334_t3_cte_param.rs`, 5 cases): `$1` inside the CTE body (the
  checklist's exact acceptance), `$1` on the outer select over a CTE, multi-param
  (`$1` body + `$2` outer), a `$1`-filtered CTE inlined into a JOIN, and the same
  `$1` reused in body + outer.
- **Codex** (`v334_t3_cte_params.rs`, 8 cases — the superset integrated here):
  all of the above plus CTE column aliases + `COUNT(*)`, `query_params_with_columns`,
  CTE referenced multiple times, **CTE in a UNION set operation**, **recursive CTE
  with params** in anchor/recursive/stop predicates, and a 200-row stress
  (5 parameterized CTE counts in ~7.2 ms).

Every compare point I flagged as a possible hiding spot (recursive CTE, CTE in
UNION, multi-reference) passed in Codex's harness.

## Integrated

Codex's `tests/v334_t3_cte_params.rs` (strict superset of my cases) + both result
files. No production source change.

## Verification (final main)

- `cargo test --test v334_t3_cte_params`: 8/8.
- `cargo test --test cte_hardening_tests`: 39/39; parameterized_query_tests 28/28.
- `cargo test --lib`: 1819 pass / 1 pre-existing hnsw / 1 ignored.
- fmt PASS; clippy only the pre-existing `streaming.rs:69`.

## Quality

Both ~88-90. The cross-agent value here was *confirming* an already-fixed verdict
robustly — two independent deep harnesses agreeing is much stronger evidence than
one shallow pass (the A5/A7 trap).
