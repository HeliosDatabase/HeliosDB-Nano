# A10 - IS DISTINCT FROM

## Verdict

ALREADY FIXED, confirmed with regression coverage only.

The checklist says `IS DISTINCT FROM` throws `Expression not yet supported: IsDistinctFrom`. On final main after A11 (`3ba2015`), the planner, evaluator, and type inference already handle both `IS DISTINCT FROM` and `IS NOT DISTINCT FROM`.

## Baseline

Baseline repo: Codex branch `fix/codex-v334-a10`, based on final main `3ba2015`.

Command:

```text
cargo test --test v334_a10_is_distinct_from -- --nocapture
```

Result: PASS from the start, 5 passed / 0 failed.

Coverage:

- Scalar truth table:
  - non-null unequal -> distinct
  - non-null equal -> not distinct
  - one NULL -> distinct
  - both NULL -> not distinct
  - `IS NOT DISTINCT FROM` inverse
- Table projection and `WHERE` filters.
- `$N` parameter operands, including `Value::Null`.
- Mixed string/int equality coercion through the existing equality helper.
- `ON CONFLICT DO UPDATE ... WHERE existing IS DISTINCT FROM excluded`.
- Stress/count smoke over 300 rows.

## Fix

No source change.

Confirmed existing implementation:

- `src/sql/planner.rs`: maps parser `Expr::IsDistinctFrom` / `Expr::IsNotDistinctFrom` to logical binary operators.
- `src/sql/evaluator.rs`: evaluates `IsDistinctFrom` / `IsNotDistinctFrom` with NULL-safe semantics.
- `src/sql/type_inference.rs`: marks both operators as boolean-producing.

## Tests

Targeted A10 regression:

```text
cargo test --test v334_a10_is_distinct_from -- --nocapture
```

Results:

- Initial run on A11 source head: 5 passed / 0 failed, 0.07s; stress COUNT over 300 rows in 2.466734ms.
- Re-run after rebasing to final main `3ba2015`: 5 passed / 0 failed, 0.06s; stress COUNT over 300 rows in 2.037838ms.

Focused compatibility:

```text
cargo fmt --check
cargo test --test ada_bug_report -- --nocapture
cargo test --test parameterized_query_tests -- --nocapture
```

Results:

- `cargo fmt --check`: clean.
- `ada_bug_report`: 3 passed / 0 failed.
- `parameterized_query_tests`: 28 passed / 0 failed.

Broad suite:

```text
cargo test --lib --quiet
```

Result: FAILED with the known unrelated HNSW test only.

- 1819 passed / 1 failed / 1 ignored.
- Failure: `vector::hnsw_index::tests::test_vector_count_tracking` at `src/vector/hnsw_index.rs:618`, expected L2 index length 3 tombstone, got 2.

Lint:

- No source change for A10, so no A10-specific clippy run.
- The A11 clippy run immediately before this item still fails on the known unrelated deny at `src/replication/streaming.rs:69:26` (`unwrap_used`).

## Claude Comparison

Claude's triage predicted this was already wired through planner, evaluator, and type inference. Codex independently confirmed that on the final A11 head with wider regression coverage, including parameters, `WHERE`, `IS NOT DISTINCT FROM`, mixed equality coercion, and an `ON CONFLICT` predicate.

Suggested final A10: test-only, union with any Claude regression tests, no source change.

## Quality Score

90/100.

Correctness is well covered for the checklist and adjacent SQL surfaces. Scope is minimal because no production code changed. Residual risk is low; this pins behavior that was already implemented.
