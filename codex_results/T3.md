# T3 Result - CTE + `$N` parameter binding returns 0 rows silently

## Verdict

ALREADY FIXED - confirmed with regression coverage only. No production code
change.

Baseline: branch `fix/codex-v334-t3` from consolidated final `main` at `6cf1566`
(A8+A14+A15+A5+A1+A7+T2).

## Baseline

`cargo test --test v334_t3_cte_params -- --nocapture`: PASS from the start.

The checklist's exact failure shape did not reproduce: `$N` references inside CTE
bodies bind to the same values as equivalent non-CTE filters.

## Root Cause Status

No active root cause found. The current planner keeps `$N` placeholders as
`LogicalExpr::Parameter { index }`, and the executor carries the same parameter
vector while materializing non-recursive and recursive CTE plans. CTE scans then
read the materialized tuples from `cte_context`.

## Tests Added

`tests/v334_t3_cte_params.rs` covers:

- `$1` inside a CTE body compared with equivalent direct `WHERE`.
- Multiple and reused parameters across CTE body and outer query (`$1`, `$2`,
  `$3`).
- CTE column aliases with `COUNT(*)`.
- `query_params_with_columns` as a second query entry point.
- Parameterized CTE joined back to the base table.
- Same `$1` reused in the CTE body and outer predicate.
- CTE referenced multiple times.
- CTE in a `UNION` set operation with parameters.
- Recursive CTE with parameters in anchor, recursive expression, and stop
  predicate.
- Bounded 200-row stress smoke.

## Performance

Targeted stress:

- 5 parameterized CTE counts over 200 rows completed in `7.216451ms` on the final
  run.

No production code changed, so there is no runtime performance impact beyond test
coverage.

## Verification

- `cargo fmt --check`: PASS.
- `cargo test --test v334_t3_cte_params -- --nocapture`: PASS, 8/8.
- `cargo test --test parameterized_query_tests -- --nocapture`: PASS, 28/28.
- `cargo test --test query_params_with_columns -- --nocapture`: PASS, 1/1.
- `cargo test --test cte_hardening_tests -- --nocapture`: PASS, 39/39.
- `git diff --check`: PASS.
- `cargo test --lib --quiet`: 1819 passed / 1 failed / 1 ignored. Failure is
  pre-existing `vector::hnsw_index::tests::test_vector_count_tracking`
  (`src/vector/hnsw_index.rs:618`, expected 3 tombstones, got 2).
- `cargo clippy --lib --quiet`: FAILS on pre-existing
  `src/replication/streaming.rs:69:26` due `unwrap_used`; no T3-specific error.

## Comparison With Claude

Claude also confirmed T3 already fixed with regression-only coverage. Claude's
compare points were recursive CTEs with params, CTE-in-set-operation with params,
and a CTE referenced multiple times; the Codex test suite includes all three and
they are green.

Suggested final integration: union of regression tests/result files, no source
change.

## Quality Score

90/100.

- Correctness: 32/35 - checklist behavior and deeper CTE shapes are green from
  baseline.
- Regression coverage: 25/25 - exact case plus recursive, set-operation,
  multi-reference, join, aliases, and query-with-columns entry point.
- Performance: 15/15 - no production code change; bounded stress recorded.
- Scope control: 15/15 - test-only.
- Residual risk: 3/10 - no bug reproduced; unsupported DML `WITH INSERT ...`
  shape was intentionally not used as T3 evidence.
